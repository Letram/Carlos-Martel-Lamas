<%= form_with(model: trip_attachment, local: true) do |form| %>
  <% if trip_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_attachment.errors.count, "error") %> prohibited this trip_attachment from being saved:</h2>

      <ul>
      <% trip_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :trip_id %>
    <%= form.number_field :trip_id, id: :trip_attachment_trip_id %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo, id: :trip_attachment_photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
